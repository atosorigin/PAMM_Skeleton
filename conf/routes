# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

GET     /                           controllers.Application.index()

# Webservice routing
# GET     /ws/mywebservicehere       controllers.mywebservicehere.method()

# UI Routing
# this maps all static resources from "public" to the root URL path; anything that hasn't been intercepted above will
# try to resolve to an asset in "public". This separates Play from the front-end.
GET     /*file                      controllers.Assets.at(path="/public", file)